# 2026-05-30

## 17:19 - 记忆继承 + 新项目 Review

### 记忆继承
- 用户提供 agent-memory 仓库地址，要求继承记忆
- 克隆遇到 TLS 问题，改用 GitHub API tarball 下载成功
- 读取全部记忆文件：MEMORY.md, USER.md, SOUL.md, AGENTS.md, TOOLS.md, memory/2026-05-29.md
- 将记忆文件复制到当前工作区
- 创建 .env.git（token base64 编码存储）
- 配置 git remote 指向 agent-memory 仓库
- 用户身份：monkey，程序员，GMT+8，腾讯云 106.54.235.209

### 新项目：college-volunteer-ai（智愿星）
- 仓库：https://github.com/11lidaden/college-volunteer-ai
- 高考志愿智能推荐系统
- 后端：FastAPI + SQLAlchemy + SQLite + Redis
- 前端：Vue 3 + Element Plus + ECharts
- 功能：智能推荐（冲/稳/保）、高校库、志愿表、等效分换算、重复检测、报考政策
- 部署：腾讯云 /gaokao/ 子路径，与论文项目共存
- 会员制 MVP：¥9.9/年，假支付

### 待办
- [ ] 重建 memory-sync cron 任务
- [x] 确认 college-volunteer-ai 是否已部署

## 17:38 - 后端 API 修复 + 部署

### 修复内容
1. **`/api/universities`** - 添加 `X-Total-Count` header（JSONResponse 方式）
2. **`/api/tools/one-segment`** - 添加 `subject_type` 过滤 + 每段增加 `count`/`cumulative` 字段
3. **新增端点**：
   - `/api/universities/data/provinces` - 获取有院校数据的省份列表
   - `/api/universities/{id}/majors` - 获取院校招生专业计划

### 部署
- 服务器：ubuntu@106.54.235.209（密码登录）
- 项目路径：/opt/college-volunteer-ai/
- 后端：gunicorn + uvicorn，端口 3002，nginx 反代 /gaokao/api/
- 用 scp 上传修改后的 university.py 和 tools.py，重启 gunicorn 生效
- API 测试全部通过：X-Total-Count=143，one-segment 返回 count/cumulative，provinces 返回省份列表

### 技术笔记
- 服务器没有 sshpass/expect，用 pexpect 库做 SSH 交互
- Python 测试脚本用 urllib（服务器 venv 没装 requests）
- 登录用 SMS 验证码，dev 模式 FAKE_SMS_CODE=888888
